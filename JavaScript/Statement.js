const nombre = "Daniel Rodríguez";
const edad = 26;

if (edad >=18 && edad <=18 || edad === 26) {
    console.log(`${nombre} es mayor de edad.`);
} else {
    console.log(`${nombre} no es mayor de edad.`);
}